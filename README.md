# React String Calculator (TDD)

A string calculator that:
- Handles `,`, `\n`, and custom delimiters
- Throws error for negative numbers
- Written with TDD + React

### Run
```bash
npm install
npm test
npm start
